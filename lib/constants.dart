import 'package:flutter/material.dart';

const kPrimaryColor = Color(0xFFe44933);

//using in booking & invoice module
const kUserBackColor = Color(0xFF2b2b2b);
const kMerchantBackColor = Color(0xFFe44933);
const kTextLightColor = Color(0xFF2b2b2b);

const kDefaultPadding = 16.0;

const kBottomSheetShape = RoundedRectangleBorder(
  borderRadius: BorderRadius.only(
      topLeft: Radius.circular(20), topRight: Radius.circular(20)),
);
